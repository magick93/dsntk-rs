[package]
name = "dmd"
version = "0.1.0"
edition = "2021"

[dependencies]
markdown-ast = "0.1.1"
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"

[dev-dependencies]
tempfile = "3.8"

[features]
default = []
